import { ComponentFixture, TestBed } from '@angular/core/testing'; import { UploadDialogComponent } from './upload-dialog.component';
const markdownFile = (name:string) => { const file=new File(['new'],name,{type:'text/markdown'}); Object.defineProperty(file,'text',{value:async()=> 'new'}); return file; };
describe('UploadDialogComponent',()=>{let f:ComponentFixture<UploadDialogComponent>;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[UploadDialogComponent]}).compileComponents();f=TestBed.createComponent(UploadDialogComponent);f.componentRef.setInput('tree',[]);f.detectChanges()});it('blocks oversized batches by UTF-8 content size',()=>{f.componentInstance.files.set([{name:'x.md',content:'x'.repeat(25000001),expectedRevision:null}]);expect(f.componentInstance.valid()).toBeFalsy()});it('counts UTF-8 encoded content bytes',()=>{f.componentInstance.files.set([{name:'x.md',content:'café',expectedRevision:null}]);expect(f.componentInstance.total()).toBe(5)});it('reports unsupported picker items',async()=>{await f.componentInstance.add([new File(['text'],'note.txt',{type:'text/plain'})]);expect(f.componentInstance.fileErrors()['note.txt']).toContain('Only Markdown')});it('requires an explicit replacement choice and uses the current revision',async()=>{const revision='r'.repeat(64);f.componentRef.setInput('tree',[{name:'existing.md',path:'existing.md',type:'file',updatedAt:'',revision}]);await f.componentInstance.add([markdownFile('existing.md')]);expect(f.componentInstance.valid()).toBeFalsy();f.componentInstance.confirmReplacement('existing.md',true);expect(f.componentInstance.valid()).toBeTruthy();expect(f.componentInstance.files()[0].expectedRevision).toBe(revision)});it('removes a collision when replacement is declined',async()=>{f.componentRef.setInput('tree',[{name:'existing.md',path:'existing.md',type:'file',updatedAt:'',revision:'r'.repeat(64)}]);await f.componentInstance.add([markdownFile('existing.md')]);f.componentInstance.confirmReplacement('existing.md',false);expect(f.componentInstance.files()).toEqual([]);expect(f.componentInstance.valid()).toBeFalsy()});});
