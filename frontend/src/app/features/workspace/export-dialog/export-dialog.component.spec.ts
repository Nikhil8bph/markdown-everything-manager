import {ComponentFixture,TestBed} from '@angular/core/testing';import {ExportDialogComponent} from './export-dialog.component';describe('ExportDialogComponent',()=>{let f:ComponentFixture<ExportDialogComponent>;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[ExportDialogComponent]}).compileComponents();f=TestBed.createComponent(ExportDialogComponent);f.componentRef.setInput('name','note.md');f.detectChanges()});it('emits export choice',()=>{let value='';f.componentInstance.exportAction.subscribe(v=>value=v);f.componentInstance.choose('html');expect(value).toBe('html')})});
